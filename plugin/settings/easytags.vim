let g:easytags_dynamic_files = 1 " will write to the first existing tags file seen by Vim (based on the 'tags' option).
                                 " Otherwise, it falls back to the global tags file

let g:easytags_auto_update=0     " disabled for performance issues
"let b:easytags_auto_highlight=0 " it seems to not affect performance
